<h1><%= @task.title%></h1>
<p><%= @task.details%></p>
<p><%= @task.completed%></p>

<%= link_to "Delete This Task",
            task_path(@task.id),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
